//Events are action that triggers js. examples:
//clicks, drags, drops, hover, scrolls, form submission, key presses, focus/blur
//mouse wheel, double click, copying, pasting, audio start, screen resize, printing

// the thing    event type     the code to run
// button       click          change the color
// input        hits return    get search results
// image        mouseover      display caption
